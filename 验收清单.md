# Story Studio 手工验收清单（M0~M4 + N1~N3）

> 自动线（每次提交前必跑，当前基线 **715 全绿**）：`npm run typecheck` 零错误；`npm run test:logic` 全绿；`npm run test:fixtures` 探针通过；`npm run build` 成功。
> 手工线（浏览器里点一遍，需一个 OpenAI 兼容端点的 Key，如 DeepSeek）：

## M0 地基
- [ ] 「设置」填创作/分析模型（baseURL/模型名/Key），两栏各点「测试」→ 流式返回 OK
- [ ] 「作品」新建作品 → F5 刷新 → 作品还在（IndexedDB 持久化）
- [ ] 「调试台」发一句话 → token 估算显示 → 流式吐字 → 「停止」能中断
- [ ] 断网/错 Key 发送 → 红行错误可读（含状态码）

## M1 ST 兼容层
- [ ] 「人物卡」导入真实 PNG 卡（工作区 素材\main_morgana...spec_v2.png）→ 名字/描述/开场白正确
- [ ] 同卡导出 JSON → 再导入 → 信息一致（roundtrip）
- [ ] 「世界书」导入 ST 全局 world info JSON → 词条数对 → 导出 ST 全局格式再导回 → 稳定
- [ ] 「匹配调试台」输入几行对话 → constant 词条必现、关键词词条按扫描窗命中、超预算词条进 dropped

## M2 构思访谈
- [ ] 新建作品后访谈页能连续聊 3 轮：每轮 AI 问一个空缺字段，回答后出现提案卡
- [ ] 采纳提案 → 左侧档案对应字段更新、进度条上升
- [ ] 改「题材」→ 下游「主线一句话」自动标黄「待复查」
- [ ] 手动编辑字段保存 → 修订记录追加；F5 后聊天历史还在

## M3 大纲工作台
- [ ] 选结构「三幕式」生成总纲 → 树出现卷/章/幕，warnings 提示未匹配角色（若有）
- [ ] 树上新建幕 → 编辑器填意图/节拍/人物 → 保存（revision+1）
- [ ] 「AI 共写」出提案 → 应用 → 节拍替换生效
- [ ] 未试跑的幕点「锁定」→ 被拦截提示；「导出 Markdown」可得 outline.md

## M4 过渡试跑闭环
- [ ] 选一幕 → 生成试跑包 → card.json/greeting.txt/note.txt/readme.md/worldbook.json 均可下载
- [ ] 把包导入真实 ST（卡+世界书），greeting 开场即本场戏 → 演 10 轮 → ST 导出 chat .jsonl
- [ ] 导回该 .jsonl → 摘要/节拍命中表/偏差/台账提案渲染正确
- [ ] 「应用节拍标记」→ 大纲树上该幕节拍打勾、状态变「已试跑」；此时才允许「锁定」
- [ ] 「反向修纲草案」→ 未中节拍附「实际：…」→ 应用生效
- [ ] 勾选台账提案 → 「采纳台账提案」→ 剧场右栏「台账」面板（或本页提案卡）确认 → 下一幕提案/共创访谈的提示词带上该事实
- [ ] 「下一幕提案」→ 尊重台账已确认事实，新幕挂在正确的章下

## N1 台账闭环（v3.1 起台账在「🎭 RP 剧场」右栏，作品工作台台账页签已删）
- [ ] 台账面板三块：待确认队列（确认/编辑/驳回/恢复）、伏笔欠账（埋×收）、正典时间线（按日、溯源、行内修正）
- [ ] 大纲幕编辑器埋一条伏笔 → 台账欠账区出现；写一条含伏笔关键词的 foreshadow 台账并确认 → 变已收
- [ ] 驳回的提案在折叠区可恢复；一切只标记不删除

## N2 站内 RP 引擎（引擎现挂在「RP 剧场」页，见 N6）
- [ ] 剧场聊天区：token 预览条分色分段，悬停看段名；把预算调到极小 → 出现「已裁旧消息/超出预算」警示且保护段仍在
- [ ] 「查看提示词」列出各段处置（被裁段划线附原因）+ 实际发送全文
- [ ] 聊长了自动出现「前情已折叠」注记；手动 🗜 也可；摘要进 token 条（保护段）
- [ ] 刷新页面 → 同幕开台显示「已恢复上一场对话」；ST 试跑页的本幕会话面板可回放/续写（跳转剧场）/标正典/弃稿
- [ ] 「重生成（换候选）」不覆盖旧回复，←/→ 回滑；任意回合 ✎ 编辑 / ✕ 删除
- [ ] 世界书带 sticky/cooldown 的词条：触发后离开关键词仍续注 N 轮，冷却期再命中被压制（与 ST 语义一致的简化版）

## N3 纠偏与状态联动（纠偏注入已并入剧场）
- [ ] 复盘偏差「→ 拟为纠偏」→ 编辑 → 「→ 移交 RP 剧场注入」→ 自动跳剧场预填 → 「🎯 注入纠偏」→ 下一句回复按纠偏走，对话不断线；超 3 条自动退旧；可清空
- [ ] 「应用节拍标记」后若全拍完成 → 出现「🔒 标记已锁定」；未 tested 时按钮说明硬规则
- [ ] 反向修纲应用后提示去剧场重开本幕；重开后 greeting 按新细纲重生成，旧会话仍可查

## N6 → v3 RP 剧场（顶层房间制；逻辑基线 693）
### P1 顶层化 + 房间管理
- [ ] 顶层出现「🎭 RP 剧场」页签（与作品/画像同级）；作品工作台里**没有**剧场页签
- [ ] 新建房间：选作品 → 全本/章选/沙盒 三选一、章多选、角色卡（含「旁白演绎」）、画像、节奏、楼层定时、借作品台账 → 开房后左栏出现该房间
- [ ] 左栏：切换即换台（对话/副本/正典跟着房间走）；改名即时生效；删除有 confirm 且其正典一并消失；⬇ 导出的 jsonl 能被试跑页「导入试跑记录」原样吃回
- [ ] 刷新页面 → 回到上次的房间，对话与进展都在；新建表单记住上次的作品/卡/画像/节奏/定时
- [ ] 作品页「🎭 导去剧场演全本」→ 自动跳顶层剧场并开全本房；试跑会话「续写（去剧场）」→ 新房间带原对话；剧场「带这些对话去复盘」→ 跳该作品并自动落在 ST 试跑页签出复盘四件套

### P2 节奏 / 双模式 / 副本进展 / 房间正典
- [ ] 「查看提示词」里能看到独立的【剧情推进节奏】段：切「紧凑」=强引导每轮抛钩子；「舒缓」=弱引导允许跑题（实际发全文对比）
- [ ] 全本房：改主纲某幕 → 房间头出现「⚡ 主纲有更新，同步副本」；不点则聊天用的仍是旧副本；点后进展清单按新纲重建且已演标记尽量保留
- [ ] 章选房：改主纲（含所选章之外与之内）→ **永远不出现**同步按钮；副本只含所选章的幕
- [ ] 副本进展面板点节拍：未演→已演→跳过→未演 三态循环，且大纲工作台上该幕节拍的勾选**毫无变化**（不回写实证）
- [ ] 新建房间正典显示「空白」；聊几楼后写进正典的事实只在**本房间**可见；另一房间同作品开台看不到它
- [ ] 勾「借作品级台账」→ 右栏「台账」面板里已确认的作品级事实在「查看提示词」里带「（以下借自作品级台账）」标注；不勾则无
- [ ] 楼层定时填 3 → 每聊满 3 个用户楼层自动触发一次场记整理（活动流出现自动条目）；填 0 不触发

### P3 场记 agent
- [ ] 「现在整理」→ 活动流逐条显示工具调用与结果首行；活动流里出现标记节拍与写正典两类动作
- [ ] 整理后：副本进展清单里对应节拍被勾选；右侧「房间正典」新增条目可删；主纲与作品级台账均无变化（append_ledger 只写房间正典）
- [ ] 面板文案明示「没有任何修改主纲的工具」；关掉「场记 agent」开关后按钮禁用、楼层定时不再触发
- [ ] 端点不支持 tools（如某些兼容网关）→ 活动流提示已降级为纯摘要整理，正典仍能写入、节拍标记不报错

### P4 本地文件夹自动写盘
- [ ] 先在磁盘预建一个空文件夹 → 房间头点「📁 连接本地文件夹」→ 选中它 → 状态变「📁 自动写入「<名>」」
- [ ] 聊一轮并等落定 → 该文件夹出现 `<房间名>-<id6>.jsonl`，内容为 ST chat 全量快照（头行+逐条）；再聊一轮 → 同文件被整体覆盖为更长版本，不产生第二个文件
- [ ] 房间改名后自动写到新文件名（旧文件保留，可手删）；两个同名房间各自独立文件（id 后缀不同）
- [ ] 在磁盘上把该文件夹删掉/移走 → 再聊一轮 → 状态变「文件夹已不可访问，请重选」，点「重选」能重新连上
- [ ] 刷新页面 → 状态变「重新连接「<名>」」一键按钮，点一下即恢复自动写（浏览器规范：授权需用户手势）
- [ ] Firefox/Safari：状态显示「此浏览器不支持…用每间「⬇」手动导出」，且左栏 ⬇ 可用（不报错、不假装自动）


## N5 项目包导出
- [ ] 作品总览「📦 导出项目包（zip）」→ 解压得到 标题目录/README.txt、outline.md/json、project.json、ledger.json、transcripts/、cards/（V2）、worldbook.json（有启用词条时）
- [ ] worldbook.json 可直接导入 ST；卡文件可直接进 ST 角色库
- [ ] 本幕会话「⬇ jsonl」→ 该 .jsonl 可直接送 st-novel-tool 做小说化；导回本页「导入试跑记录」也能原样吃回

## 已知边界（非缺陷）
- 第三方网关不开 CORS 时浏览器直连失败 → 桌面壳阶段解决
- PNG 卡仅支持 tEXt `chara`（ccv3/iTXt 形态留扩展）
- 世界书 group 为确定性简化（同组留权重最大），非 ST 的加权随机
- v3：多标签页同房间写同一文件夹 → 后落盘者覆盖（不做跨页锁）
- v3：刷新后 FSA 写权限回到 prompt，需点一次「重新连接」（浏览器规范，非实现缺陷）
- v3：场记 append_ledger 直写本房间正典；作品级台账的裁决在剧场右栏「台账」面板人工在环
- v3：节拍命中复盘属 ST 试跑页；沙盒房无剧本，复盘不适用（用 ⬇ jsonl 导出）
- v3.1：同一房间在两个浏览器页签同时聊，消息仍可能互相覆盖（**请勿双开同一房间**）
- v3.1：进度记忆走 localStorage（只存 id 与小段草稿），隐私模式/清站点数据即失效

## v3.1 七项体验修补（逻辑基线 715）
### ① 场记切页不断 + 截断修复
- [ ] 楼层定时触发或点「现在整理」→ 活动流跑到一半时切去「作品」页再切回 → **活动流还在继续滚、步骤一条不少**（不重开、不丢步骤）；整理完前房间左栏切别的房间再回来也一样
- [ ] 整理过程中逐条看活动流：每步「工具名 → 结果首行」出现的同时，右栏房间正典/进展清单**已经**同步变化（即时落库，不等收尾）
- [ ] 分析端点把 max_tokens 设很小（如 512）→ 活动流该步出现「被 max_tokens 截断」标注而不是静默空结果
### ② 进度记忆 + 清除
- [ ] 大纲页选中某幕 → 切作品 → 切回 → 还是选中原幕；共创输入框打到一半没提交，切走再回草稿还在
- [ ] 人物/世界书/画像页展开编辑的卡：切页回来仍是展开态；访谈输入与行内编辑草稿同理
- [ ] 试跑页选幕/选主角/选画像 + 输入草稿，切页回来都在
- [ ] 设置「🧹 清除进度记忆」→ 点一下 → 再切页不恢复（且提示清除条数）；剧场新建表单记忆（prefs）一并清
### ③ 预算自动记忆
- [ ] 房间 A 把 token 预算/预留回复改成 3000/800 → 等半秒 → 切房间再切回 → 数字还在；刷新页面也还在
### ④ 台账迁入剧场（每房间独立）
- [ ] 作品工作台**没有**「台账」页签了；剧场右栏「台账」面板三段齐全（房间正典/作品级台账/伏笔欠账）
- [ ] 房间 A 正典写几条 → 同作品房间 B 的台账面板里看不到它们；作品级区两房间看到同一份
- [ ] 作品级「待确认」在剧场面板里可直接确认/编辑/驳回；已确认条目点「引入本房间」→ 房间正典出现副本、作品级原条不动
### ⑤ 用户名=画像
- [ ] 设置页**没有**全局用户名输入框（只有一行说明）；新建房间选画像「小明」→ 聊天中 {{user}} 显示「小明」；不选画像 → 显示「读者」
### ⑥ 对话历史保护
- [ ] AI 正在吐字时直接刷新页面 → 回来后半句/全文都在（不空房）；吐字时切去别的页签再回来同样不丢
- [ ] 生成中改台面装配（如注入纠偏）→ 不弹清空、正在生成的回复保留原现场
- [ ] 「重新开始」与「删房间」「删作品」都有确认框，文案写明波及范围
### ⑦ 折叠思维链 + 随轮自动场记
- [ ] assistant 回复下方「🧠 思考…」默认**折叠**，点开见思维链原文 + 场记活动逐条；刷新后仍在（持久化）；模型不吐 reasoning 时该行不出现
- [ ] 楼层定时=1（默认）：每发一楼自动跑一次场记；「查看提示词」里每条回复都带「【进展备忘】第 X/N 幕 · 拍 m/n；下一拍：…」一行
- [ ] 场记自动标记的节拍在副本进展面板实时打勾；关掉「场记 agent」后一切停发
