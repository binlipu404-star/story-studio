# Story Studio 手工验收清单（M0~M4 + N1~N3）

> 自动线（每次提交前必跑，当前基线 **931 全绿**）：`npm run typecheck` 零错误；`npm run test:logic` 全绿；`npm run test:fixtures` 探针通过；`npm run build` 成功。
> 手工线（浏览器里点一遍，需一个 OpenAI 兼容端点的 Key，如 DeepSeek）：

## M0 地基
- [ ] 「设置」填创作/分析模型（baseURL/模型名/Key），两栏各点「测试」→ 流式返回 OK
- [ ] 「作品」新建作品 → F5 刷新 → 作品还在（IndexedDB 持久化）
- [ ] 「调试台」发一句话 → token 估算显示 → 流式吐字 → 「停止」能中断
- [ ] 断网/错 Key 发送 → 红行错误可读（含状态码）

## M1 ST 兼容层
- [ ] 「人物卡」导入真实 PNG 卡（任一张 V2 内嵌卡即可，如设了 `SS_TEST_PNG` 指向的那张）→ 名字/描述/开场白正确
- [ ] 同卡导出 JSON → 再导入 → 信息一致（roundtrip）
- [ ] 「世界书」导入 ST 全局 world info JSON → 词条数对 → 导出 ST 全局格式再导回 → 稳定
- [ ] 「匹配调试台」输入几行对话 → constant 词条必现、关键词词条按扫描窗命中、超预算词条进 dropped

## M2 构思访谈
- [ ] 新建作品后访谈页能连续聊 3 轮：每轮 AI 问一个空缺字段，回答后出现提案卡
- [ ] 采纳提案 → 左侧档案对应字段更新、进度条上升
- [ ] 改「题材」→ 下游「主线一句话」自动标黄「待复查」
- [ ] 手动编辑字段保存 → 修订记录追加；F5 后聊天历史还在

## M3 大纲工作台
- [ ] 选结构「三幕式」生成总纲 → 树出现卷/章/幕，warnings 提示未匹配角色（若有）
- [ ] 树上新建幕 → 编辑器填意图/节拍/人物 → 保存（revision+1）
- [ ] 「AI 共写」出提案 → 应用 → 节拍替换生效
- [ ] 未试跑的幕点「锁定」→ 被拦截提示；「导出 Markdown」可得 outline.md

## M4 过渡试跑闭环
- [ ] 选一幕 → 生成试跑包 → card.json/greeting.txt/note.txt/readme.md/worldbook.json 均可下载
- [ ] 把包导入真实 ST（卡+世界书），greeting 开场即本场戏 → 演 10 轮 → ST 导出 chat .jsonl
- [ ] 导回该 .jsonl → 摘要/节拍命中表/偏差/台账提案渲染正确
- [ ] 「应用节拍标记」→ 大纲树上该幕节拍打勾、状态变「已试跑」；此时才允许「锁定」
- [ ] 「反向修纲草案」→ 未中节拍附「实际：…」→ 应用生效
- [ ] 勾选台账提案 → 「采纳台账提案」→ 剧场右栏「台账」面板（或本页提案卡）确认 → 下一幕提案/共创访谈的提示词带上该事实
- [ ] 「下一幕提案」→ 尊重台账已确认事实，新幕挂在正确的章下

## N1 台账闭环（v3.1 起台账在「🎭 RP 剧场」右栏，作品工作台台账页签已删）
- [ ] 台账面板三块：待确认队列（确认/编辑/驳回/恢复）、伏笔欠账（埋×收）、正典时间线（按日、溯源、行内修正）
- [ ] 大纲幕编辑器埋一条伏笔 → 台账欠账区出现；写一条含伏笔关键词的 foreshadow 台账并确认 → 变已收
- [ ] 驳回的提案在折叠区可恢复；一切只标记不删除

## N2 站内 RP 引擎（引擎现挂在「RP 剧场」页，见 N6）
- [ ] 剧场聊天区：token 预览条分色分段，悬停看段名；把预算调到极小 → 出现「已裁旧消息/超出预算」警示且保护段仍在
- [ ] 「查看提示词」列出各段处置（被裁段划线附原因）+ 实际发送全文
- [ ] 聊长了自动出现「前情已折叠」注记；手动 🗜 也可；摘要进 token 条（保护段）
- [ ] 刷新页面 → 同幕开台显示「已恢复上一场对话」；ST 试跑页的本幕会话面板可回放/续写（跳转剧场）/标正典/弃稿
- [ ] 「重生成（换候选）」不覆盖旧回复，←/→ 回滑；任意回合 ✎ 编辑 / ✕ 删除
- [ ] 世界书带 sticky/cooldown 的词条：触发后离开关键词仍续注 N 轮，冷却期再命中被压制（与 ST 语义一致的简化版）

## N3 纠偏与状态联动（纠偏注入已并入剧场）
- [ ] 复盘偏差「→ 拟为纠偏」→ 编辑 → 「→ 移交 RP 剧场注入」→ 自动跳剧场预填 → 「🎯 注入纠偏」→ 下一句回复按纠偏走，对话不断线；超 3 条自动退旧；可清空
- [ ] 「应用节拍标记」后若全拍完成 → 出现「🔒 标记已锁定」；未 tested 时按钮说明硬规则
- [ ] 反向修纲应用后提示去剧场重开本幕；重开后 greeting 按新细纲重生成，旧会话仍可查

## N6 → v3 RP 剧场（顶层剧组制；逻辑基线 693）
### P1 顶层化 + 剧组管理
- [ ] 顶层出现「🎭 RP 剧场」页签（与作品/画像同级）；作品工作台里**没有**剧场页签
- [ ] 新建剧组：选作品 → 全本/章选/沙盒 三选一、章多选、角色卡（含「旁白演绎」）、画像、节奏、楼层定时、借作品台账 → 开机后左栏出现该剧组
- [ ] 左栏：切换即换台（对话/副本/正典跟着剧组走）；改名即时生效；删除有 confirm 且其正典一并消失；⬇ 导出的 jsonl 能被试跑页「导入试跑记录」原样吃回
- [ ] 刷新页面 → 回到上次的剧组，对话与进展都在；新建表单记住上次的作品/卡/画像/节奏/定时
- [ ] 作品页「🎭 导去剧场演全本」→ 自动跳顶层剧场并开全本组；试跑会话「续写（去剧场）」→ 新剧组带原对话；剧场「带这些对话去复盘」→ 跳该作品并自动落在 ST 试跑页签出复盘四件套

### P2 节奏 / 双模式 / 副本进展 / 剧组正典
- [ ] 「查看提示词」里能看到独立的【剧情推进节奏】段：切「紧凑」=强引导每轮抛钩子；「舒缓」=弱引导允许跑题（实际发全文对比）
- [ ] 全本组：改主纲某幕 → 剧组头出现「⚡ 主纲有更新，同步副本」；不点则聊天用的仍是旧副本；点后进展清单按新纲重建且已演标记尽量保留
- [ ] 章选组：改主纲（含所选章之外与之内）→ **永远不出现**同步按钮；副本只含所选章的幕
- [ ] 副本进展面板点节拍：未演→已演→跳过→未演 三态循环，且大纲工作台上该幕节拍的勾选**毫无变化**（不回写实证）
- [ ] 新建剧组正典显示「空白」；聊几楼后写进正典的事实只在**本剧组**可见；另一剧组同作品开台看不到它
- [ ] 勾「借作品级台账」→ 右栏「台账」面板里已确认的作品级事实在「查看提示词」里带「（以下借自作品级台账）」标注；不勾则无
- [ ] 楼层定时填 3 → 每聊满 3 个用户楼层自动触发一次场记整理（活动流出现自动条目）；填 0 不触发

### P3 场记 agent
- [ ] 「现在整理」→ 活动流逐条显示工具调用与结果首行；活动流里出现标记节拍与写正典两类动作
- [ ] 整理后：副本进展清单里对应节拍被勾选；右侧「剧组正典」新增条目可删；主纲与作品级台账均无变化（append_ledger 只写剧组正典）
- [ ] 面板文案明示「没有任何修改主纲的工具」；关掉「场记 agent」开关后按钮禁用、楼层定时不再触发
- [ ] 端点不支持 tools（如某些兼容网关）→ 活动流提示已降级为纯摘要整理，正典仍能写入、节拍标记不报错

### P4 本地文件夹自动写盘
- [ ] 先在磁盘预建一个空文件夹 → 剧组头点「📁 连接本地文件夹」→ 选中它 → 状态变「📁 自动写入「<名>」」
- [ ] 聊一轮并等落定 → 该文件夹出现 `<剧组名>-<id6>.jsonl`，内容为 ST chat 全量快照（头行+逐条）；再聊一轮 → 同文件被整体覆盖为更长版本，不产生第二个文件
- [ ] 剧组改名后自动写到新文件名（旧文件保留，可手删）；两个同名剧组各自独立文件（id 后缀不同）
- [ ] 在磁盘上把该文件夹删掉/移走 → 再聊一轮 → 状态变「文件夹已不可访问，请重选」，点「重选」能重新连上
- [ ] 刷新页面 → 状态变「重新连接「<名>」」一键按钮，点一下即恢复自动写（浏览器规范：授权需用户手势）
- [ ] Firefox/Safari：状态显示「此浏览器不支持…用每间「⬇」手动导出」，且左栏 ⬇ 可用（不报错、不假装自动）


## N5 项目包导出
- [ ] 作品总览「📦 导出项目包（zip）」→ 解压得到 标题目录/README.txt、outline.md/json、project.json、ledger.json、transcripts/、cards/（V2）、worldbook.json（有启用词条时）
- [ ] worldbook.json 可直接导入 ST；卡文件可直接进 ST 角色库
- [ ] 本幕会话「⬇ jsonl」→ 该 .jsonl 可直接送 st-novel-tool 做小说化；导回本页「导入试跑记录」也能原样吃回

## 已知边界（非缺陷）
- 第三方网关不开 CORS 时浏览器直连失败 → 桌面壳阶段解决
- PNG 卡仅支持 tEXt `chara`（ccv3/iTXt 形态留扩展）
- 世界书 group 为确定性简化（同组留权重最大），非 ST 的加权随机
- v3：多标签页同剧组写同一文件夹 → 后落盘者覆盖（不做跨页锁）
- v3：刷新后 FSA 写权限回到 prompt，需点一次「重新连接」（浏览器规范，非实现缺陷）
- v3：场记 append_ledger 直写本剧组正典；作品级台账的裁决在剧场右栏「台账」面板人工在环
- v3：节拍命中复盘属 ST 试跑页；沙盒组无剧本，复盘不适用（用 ⬇ jsonl 导出）
- v3.1：同一剧组双开——后开页签自动整组只读（navigator.locks，同浏览器内）；锁 API 不可用的浏览器不拦，**请勿双开同一剧组**
- v3.1：进度记忆走 localStorage（只存 id 与小段草稿），隐私模式/清站点数据即失效

## v3.1 七项体验修补（逻辑基线 715）
### ① 场记切页不断 + 截断修复
- [ ] 楼层定时触发或点「现在整理」→ 活动流跑到一半时切去「作品」页再切回 → **活动流还在继续滚、步骤一条不少**（不重开、不丢步骤）；整理完前剧组左栏切别的剧组再回来也一样
- [ ] 整理过程中逐条看活动流：每步「工具名 → 结果首行」出现的同时，右栏剧组正典/进展清单**已经**同步变化（即时落库，不等收尾）
- [ ] 分析端点把 max_tokens 设很小（如 512）→ 活动流该步出现「被 max_tokens 截断」标注而不是静默空结果
### ② 进度记忆 + 清除
- [ ] 大纲页选中某幕 → 切作品 → 切回 → 还是选中原幕；共创输入框打到一半没提交，切走再回草稿还在
- [ ] 人物/世界书/画像页展开编辑的卡：切页回来仍是展开态；访谈输入与行内编辑草稿同理
- [ ] 试跑页选幕/选主角/选画像 + 输入草稿，切页回来都在
- [ ] 设置「🧹 清除进度记忆」→ 点一下 → 再切页不恢复（且提示清除条数）；剧场新建表单记忆（prefs）一并清
### ③ 预算自动记忆
- [ ] 剧组 A 把 token 预算/预留回复改成 3000/800 → 等半秒 → 切剧组再切回 → 数字还在；刷新页面也还在
### ④ 台账迁入剧场（每剧组独立）
- [ ] 作品工作台**没有**「台账」页签了；剧场右栏「台账」面板三段齐全（剧组正典/作品级台账/伏笔欠账）
- [ ] 剧组 A 正典写几条 → 同作品剧组 B 的台账面板里看不到它们；作品级区两剧组看到同一份
- [ ] 作品级「待确认」在剧场面板里可直接确认/编辑/驳回；已确认条目点「引入本剧组」→ 剧组正典出现副本、作品级原条不动
### ⑤ 用户名=画像
- [ ] 设置页**没有**全局用户名输入框（只有一行说明）；新建剧组选画像「小明」→ 聊天中 {{user}} 显示「小明」；不选画像 → 显示「读者」
### ⑥ 对话历史保护
- [ ] AI 正在吐字时直接刷新页面 → 回来后半句/全文都在（不空组）；吐字时切去别的页签再回来同样不丢
- [ ] 生成中改台面装配（如注入纠偏）→ 不弹清空、正在生成的回复保留原现场
- [ ] 「重新开始」与「删剧组」「删作品」都有确认框，文案写明波及范围；「删除末条」同样有确认
- [ ] 删剧组时浏览器自动先下载一份该剧组 jsonl 留底，然后剧组才消失
- [ ] 同一剧组开两个标签页：后开的那个整组只读并出现 🔒 横幅；关掉它，原页签照常可写
- [ ] 触发一次「🗜 压缩前情」后查库（或再次刷新确认 UI 无旧消息复活）；被折叠的旧回合不再出现在聊天里，但剧组数据 `messagesArchive` 里有底
### ⑦ 折叠思维链 + 随轮自动场记
- [ ] assistant 回复下方「🧠 思考…」默认**折叠**，点开见思维链原文 + 场记活动逐条；刷新后仍在（持久化）；模型不吐 reasoning 时该行不出现
- [ ] 楼层定时=1（默认）：每发一楼自动跑一次场记；「查看提示词」里每条回复都带「【进展备忘】第 X/N 幕 · 拍 m/n；下一拍：…」一行
- [ ] 场记自动标记的节拍在副本进展面板实时打勾；关掉「场记 agent」后一切停发

## v3.2 构思档案入剧场 + 访谈辅助选材（逻辑基线 740）

### ⑧ 构思档案要素固定注入 RP 剧场
- [ ] 作品的构思档案填上若干字段（如 题材与类型/世界规则/主角与欲望-障碍），进 RP 剧场点「查看提示词」：system 里出现「【构思档案·作品定位】」段，逐条列出**有值**字段（空字段不出现）
- [ ] 该段位于「世界书·背景」段**之前**（可缓存前缀区）；预算面板里它显示为「构思档案」，颜色独立
- [ ] 沙盒组（自由即兴）同样能看到该段；无剧本组、有主役卡组、旁白组三条路径都在
- [ ] 把某字段值改到 200 字以上：注入文本里该条截到 160 字加省略号；档案全空时整段消失
- [ ] 改档案字段后再发消息，「查看提示词」里该段同步更新（每轮现装配，无需重开剧组）
- [ ] 极端小预算（预算调到刚好不够）：被裁的第一段是「构思档案」，剧组正典与剧本副本仍在

### ⑨ 构思访谈的辅助选材
- [ ] 作品工作区 → 构思访谈：右栏「构思访谈」标题下出现「辅助选材」折叠条，展开可见三列（人物卡 / 世界书词条 / 用户人设）；库为空时该列显示"××为空"
- [ ] 勾选一个人物卡 + 一个世界书词条 + 一个人设，标题行摘要变成「已选 3 项」；发一轮后「查看提示词」（或浏览器 Network 看请求体）里出现第二条 system 消息「【辅助选材·作者指定】」，含《名字》与正文，世界书正文超 300 字被截断
- [ ] 不勾选时**只有**原来那条 system 消息（不产生多余 system 条）
- [ ] 刷新页面：勾选仍在（进度记忆）；把某素材删掉再刷新：勾选自动脱落、不报错
- [ ] 流式回复过程中复选框禁用（避免同轮中途改注入内容）
- [ ] 换作品（切 projectId）：候选清单跟着换，不串作品的旧勾选

## v4 人物卡/世界书全局化 + 规模化 UI（逻辑基线 756）

### ⑩ 顶层菜单与全局库
- [ ] 顶栏七个页签齐全：作品 / 🎭 RP 剧场 / 👤 人物卡 / 📚 世界书 / 画像 / 设置 / 调试台；人物卡与世界书不再出现在作品工作台页签里（工作区第四个页签是「📦 资产」）
- [ ] 顶层「👤 人物卡」页：＋ 新建人物卡 → 建出的是**全局卡**（主场徽标=全局）；导入 ST 卡同样落全局库；行上有 名称过滤 + 主场筛选 + 「全局卡库 N 张，当前视图 M 张」计数
- [ ] 顶层「📚 世界书」页：新增/导入词条都落全局库；行上有 词条名/触发词 过滤 + 主场筛选 + 只看启用；匹配调试台用的是**本页临时参数**（改它不影响任何作品的实际注入参数）（**v5 已改版为书卡列表，见 v5 段**）
- [ ] 作品的「📦 资产」页签：两列（人物卡/世界书词条）带搜索框；本作品自有的条目勾选框打勾且**禁用**、标「自有」；外部全局条目可勾选 → 刷新页面勾选仍在（写进 Project.castIds/loreIds）（**v5：世界书列改为整本勾选，写 Project.loreBookIds，见 v5 段**）
- [ ] 勾选某全局卡后：该作品的 大纲「出场人物」/ 试跑主演下拉 / 访谈辅助选材 / 剧场新建剧组的角色卡下拉 里都能选到它；取消勾选后消失（剧场需重开或切组刷新，装配按开台时快照）
- [ ] 世界书参数（scanDepth/tokenBudget/递归）在「📦 资产」里改，保存后刷新仍在；旧世界书页里的参数面板已移除

### ⑪ 全局资产的删除语义（重点回归）
- [ ] 删除一张被 2 个作品使用的全局卡：confirm 文案出现「正被 2 个作品使用」；确认后两个作品的选用列表都摘掉它（各自「📦 资产」里不再出现），另一作品的自有卡不受影响
- [ ] 删除作品：confirm 文案明确「人物卡与世界书**不会**随作品删除」；删后全局库里该作品主场的卡/词条**仍在**（主场徽标显示「（已删作品）」），其它作品对它们的选用关系自动失效不报错
- [ ] 全局页「视图内全部启用/禁用/删除」按钮上带条数，且只作用于**当前过滤视图**；无过滤时 confirm 里明说「当前视图=整个全局库」
- [ ] 旧数据兼容（v3 库直接跑 v4 代码）：老作品的自有卡/词条照常可见可用，无需任何迁移操作

### ⑫ 规模化 UI（按 100+ 卡 / 500+ 词条自查，可用脚本批量造数据）
- [ ] 名称很长的卡/词条（导入一张英文长名 ST 卡、造一条 comment 极长的词条）：列表行名称省略号截断，行尾「导出卡/删除」按钮不被推出面板，页面不横向滚动
- [ ] 剧场左栏（232px）里作品下拉、角色卡下拉、画像下拉：选项名字很长时不溢出到右侧聊天区
- [ ] 全局页 500 条时编辑一条词条的 content：输入不掉帧（其他行不跟着重渲）；删除确认框里的名字被截到 20 字
- [ ] 调试台「复制注入文本」在剪贴板被拒时：页内出现只读 textarea 供手动全选复制，**不再**弹一个超宽的 prompt 单行框
- [ ] 剧场「带长名剧组」删除/重命名弹窗可读；作品列表长标题按钮省略号且不顶掉「删除」按钮

## v5 世界书整本书化（逻辑基线 787；schema 零迁移）

### ⑬ 书卡列表与导入
- [ ] 旧库首开自动迁移：升级后打开「📚 世界书」，原散装词条收进一本「旧版词条」书；工具条计数显示「世界书 1 本 · 词条共 N 条」，不出现「未归书的散装词条」黄条
- [ ] 点「导入世界书(.json)」选一份 ST world info：确认框文案含《书名》+ 条数 + 「一整本世界书 / 不拆散 / 作品整本选用」字样；确认后自动展开新书，词条数与文件一致、order 为 0..N-1
- [ ] 连导两份同名文件：生成两本独立书（id 不同、词条不混）；导入角色卡内嵌 character_book 同样成书（书名取卡内 name，缺失则为《卡名》内嵌世界书）
- [ ] 「新建世界书」prompt 取名 → 出现空书；展开「＋ 新增词条」写一条带 keys 的词条 → 保存后徽标显示触发词与启用态
- [ ] 书头操作齐全：展开/收起、整本启用↔停用（停用出「整本已停用」徽标 + 提示语）、改名（prompt 预填当前名）、导出ST书（启用词条，`{entries:{uid:{...}}}` 全局格式）、导出卡内嵌（全部词条 + 书参数 character_book 格式）、删除整本
- [ ] 导出文件名：书名里的 `\/:*?"<>|` 全部替换为 `_`；空书名落 `lorebook.json`
- [ ] 书内逐条编辑：keys/副键/content/order/五个开关可改可保存可重置；↑↓ 与本书视图内相邻行交换 order，不跳到别的书
- [ ] 书内批量（作用域=本书视图）：全部启用/禁用、删除视图内词条，按钮带条数且 confirm 点名书名
- [ ] 匹配调试台：池 =「整本启用书籍的全部词条」；把某书整本停用后池计数立刻变小，跑匹配不再出现该书内容；概率骰重跑可能不同

### ⑭ 作品侧整本选用与注入语义
- [ ] 作品「📦 资产」📚 列标题为「世界书（整本选用）」，每行=一本书（badge「N 条 / 启用 M」），勾选 → 刷新仍在（写 Project.loreBookIds）
- [ ] 勾两本书 → 剧场装配/试跑 worldbook.json/打包 bundle 里能看到两本内容；调换勾选顺序 → 注入顺序按书序变（可看 worldbook.json 键序或提示词）
- [ ] 取消勾选一本书：上述注入源立即只剩另一本（剧场需切组/重开刷新，装配按开台快照）
- [ ] 「📦 资产」里整本停用某书 → 任意作品的可见词条（大纲/访谈选材/试跑/剧场装配）都不再出现该书内容；恢复启用后回来（AND 语义：整本开关 ∧ 词条 enabled）
- [ ] 删除整本：confirm 含条数与「正被 N 个作品选用」；确认后全局库书消失、各作品勾选自动解除、不报错
- [ ] 旧数据兼容：v4 选过词条的作品迁移后自动选中「旧版词条」（可见只增不减）；从未用词的作品不误选
- [ ] 删除整本「旧版词条」后，老作品选用列表自动摘除引用，其它作品不受影响

## v6 大纲工作台重构（逻辑基线 864；schema 零迁移）

### ⑮ 手动编写：拖拽排序与结构编辑
- [ ] 顶层两视图「✍️ 手动编写 / 💬 对话创作」，切到哪个下次回来还是哪个（进度记忆）；工具条只剩「＋ 新卷」「⬇ 导出」「N 个节点」，没有生成总纲/下一幕提案/填充细纲等 AI 按钮
- [ ] 每行行首有拖拽柄 ⠿：拖某幕到同章另一幕**上缘**→ 插到其前；**下缘**→ 插到其后；**中段**→ 成为该幕的子级（若层级非法则拒绝并黄条给原因）
- [ ] 跨章拖：把章2 的幕拖进章3 → 该幕出现在章3 末尾，章2 剩下的幕 order 连续无空洞
- [ ] 拖卷到树空白处 → 成为新根级卷（顺序在末尾）；把章/幕拖到空白处 → 拒绝并提示「空白区只接受把「卷」投为新的根级卷」
- [ ] 拖不动自己：把卷拖到自己或自己的章/幕上 → 拒绝「不能拖到自己或自己的子树上」
- [ ] 拖拽中途松手在页面空白处/被拒绝 → 插入线与高亮消失，不留残留样式
- [ ] 换序后立刻反映到叙事顺序：导出 Markdown 的幕序、试跑「上一幕」、剧场幕序都跟着变（换序=改叙事顺序）
- [ ] 编辑器「移动到…」：幕可投到任意章、章可投到任意卷（选项不含自己与自己的子树），效果同拖到中段
- [ ] 拖拽/移动**不打断编辑**：右栏正改着某幕正文未保存，此时拖另一行 → 右栏内容不丢、不被重挂载
- [ ] 编辑器不再有「AI 共写提案」按钮；标题/意图/地点/时间/角色/节拍/伏笔/状态/删除子树一切照旧可用
- [ ] 「⬇ 导出」菜单三项：大纲 Markdown 直接下载；剧情世界书（勾幕成书）展开原勾幕面板可导出；从对话草稿导出世界书在无草稿时置灰

### ⑯ 对话创作：整页访谈与真实创作可见
- [ ] 左聊天右预览两列：聊起来后右侧实时长出只读草稿树（📚卷→📖章→🎬幕，含一句话目标），计数行「N卷 · M章 · K幕」
- [ ] 每轮 assistant 气泡下显示「本轮草稿变化：+N卷 +M章 +K幕」（增绿减红），无变化时显示「本轮草稿无变更」
- [ ] **戳穿空口白话**：若 AI 回复说"已生成/已完成"而草稿无变化 → 气泡下黄条「AI 声称完成，但草稿没有任何变化」并给出[重试本轮]
- [ ] 本轮 JSON 坏了 → 保留上一版草稿 + 提示解析失败，右侧树不塌不回空
- [ ] 刷新页面/切走再回来：对话历史与草稿**还在**（截尾 60 条），输入框半截话也在
- [ ] 「✅ 应用到大纲树…」弹窗列出「将追加 N 卷 / M 章 / K 幕」明细，勾选「应用后立即为这些空幕生成节拍」后新幕自动长出节拍；中途[停止] → 已写入的幕保留
- [ ] AI 说 ready 时只是提示，**不会**自动写入大纲树——必须本人点弹窗确认
- [ ] 「🥁 导出为世界书」不建树也能把当前草稿导出成剧情推进世界书 JSON

### ⑰ 构思访谈：提案才是创作
- [ ] 每轮 assistant 若有提案，卡片标题旁标注「勾选并「采纳」后才真正写入档案」
- [ ] **戳穿空口白话**：AI 回复含"已写入/已完成"却没有任何提案 → 气泡下黄条「AI 声称完成，但本轮没有任何字段提案——档案未发生创作」+ [重发上一条]（点它即重发该气泡对应的用户原话）
- [ ] 历史气泡同样检测：重开页面回看旧对话，空口宣称的那几条照样挂着黄条
- [ ] AI 把 JSON 吐成裸文本（含 "updates" 字样）且无提案时，额外一行小字说明「本轮 JSON 解析失败，未形成提案」
- [ ] 正常轮次不误伤：回复里说"还没有完成""这样写行吗？""本轮没有提案"时不出现黄条
- [ ] 采纳落库链路不变：勾选→采纳→左栏档案与完成度更新、旧值进 revisions、stale 传导照旧

## v6.2 端点预设（逻辑基线 900）
- [ ] 设置页「预设」：填好端点→输名字→「＋ 存为预设」→ 下拉出现该项且带「（使用中）」标记
- [ ] 换一套端点保存后，下拉点「▶ 启用」→ 两槽位 baseURL/模型/Key 全部回到存快照那一刻
- [ ] 「⤓ 覆盖」= 用当前设置覆盖同名预设（不新增条目）；「🗑 删除」只删预设，不动当前设置
- [ ] 同名字再次「＋ 存为预设」= 覆盖旧值；刷新页面预设还在（localStorage）
- [ ] 浏览器版不显示「桌面版」面板（isTauri 判断）；桌面版才见代理端口与「🔄 检查更新」

## v7 桌面版（逻辑基线 931；需安装 Story Studio_x64-setup.exe）
### A1 壳基座
- [ ] 双击安装包 → 无需管理员 → 开始菜单/桌面出现 Story Studio；重开应用窗口尺寸位置复原（窗口状态记忆）
- [ ] 重复双击图标 → 不出现第二个窗口，已有窗口被唤到前台（单实例）
- [ ] 作品/设置数据与浏览器版相互独立（不同 identifier 目录），F5 级刷新不丢数据
### A2 本地代理
- [ ] 设置「桌面版」显示「本地代理端口：<非 0 数字>」；点「一键把端点改走本地代理」→ 两槽位 baseURL 变成 `http://127.0.0.1:<端口>/proxy/https/<网关域名>/v1` → 保存
- [ ] 两栏「测试」→ **逐字流式**出字（不是整段一次出现）；生成中点「停止」→ 真的立即停
- [ ] 第三方网关（零 CORS、OPTIONS 403 那种）也能正常测试 = 代理根治生效
### A3 迁移桥
- [ ] 全局面板「📦 导出全部数据(.json)」→ 文件含 projects/outlineNodes/characters/loreEntries/sessions/ledger/meta/personas 八表与条数
- [ ] 浏览器版同样导出 → 桌面版「📥 导入转储文件」→ 弹窗列出各表条数；「取消」=合并（同 id 覆盖）、「确定」=整库替换
- [ ] 导入后大纲树/台账/世界书/人物全部可见；拿一个改坏的文件（删掉 projects 字段）导入 → 报「转储校验未通过」并指出缺表，不写脏数据
### B1 自动更新
- [ ] 设置「桌面版」「🔄 检查更新」：已是最新时回「已是最新版本」；有新版时显示版本号 + MB 下载计数 → 进度条装完 → 自动重启 → 版本号变大且书稿完好
- [ ] 断网/镜像未同步时点检查更新 → 只回一句「更新失败（不影响使用…）」，程序照常可用（失败静默）
- [ ] 镜像覆盖（GitHub 不通时自救）：在 `%APPDATA%\town.cafero.storystudio\` 建 `update-mirror.txt`，每行写一个更新清单 URL（`#` 起头为注释）→ 重启后「检查更新」改从该源检查；删掉文件即回落 GitHub 双源
### B2 CI 发布
- [ ] 打 tag `v*` 推送 → Actions 的 Release Desktop 绿 → Release 页出现 `Story.Studio_<版本>_x64-setup.exe`、`.exe.sig`、`latest.json` 三件（无需手工操作）
- [ ] 演练场（今晚可用）：`update-mirror.txt` 填 `http://127.0.0.1:5198/latest.json`，开发窗口点检查更新 → 应见版本 **9.9.9**（镜像挂的是 CI v0.1.1 真包真签名）；验完删掉该文件即恢复
